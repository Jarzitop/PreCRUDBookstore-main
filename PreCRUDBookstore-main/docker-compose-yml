version: "3.9"
services:
  backend:
    build:
      dockerfile: Dockerfile                               
    ports:
      - "8080:8080"

  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      BACKEND_URL: "http://backend:8080"
    depends_on:
      - backend
